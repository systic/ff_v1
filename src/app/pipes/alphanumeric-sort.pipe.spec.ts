import { AlphanumericSortPipe } from './alphanumeric-sort.pipe';

describe('AlphanumericSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphanumericSortPipe();
    expect(pipe).toBeTruthy();
  });
});
